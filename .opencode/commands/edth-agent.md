---
description: Turn a CSV of problem statements into a winning pitch deck. Subcommands: dry-run, run, team, validate, sheet, panel, reset, skip-to, rerun, render, status, help.
---

Execute the edth-agent workflow with subcommand: $ARGUMENTS

Follow the EDTH Hackathon Agent skill instructions exactly. Load state from artefacts/state.json if it exists. Run all Python commands with `uv run python`. Write artefacts to artefacts/.
